work on
	- sessionAuth class
	- work on crud builder
work on whenType and others on bootstrap

service provider->bindArguments should be passed through the method loader

auth module/classes
	- validate registration credentials
	- send mail and create new user
	- visit email verification link

ready for controller actions!


install eloquent and plates

->createQueryBuilder()

->select('u')->from(User::class, 'u')

->where('u.email = ?1')->setParameter(1, $reqData['email'])

route caching
	- all the routes should pass through regexForm and get saved with the result under a trie the way they were defined. The aim is to skip this replacement phase during runtime
	- only needed if you're using a lot of dynamic routes

meeting at the village jan 29th
burial date feb 25th